.row
  .span5
    %h2 Logg inn som bruker
    = semantic_form_for User.new(:email => @user_mail), :url => login_path, :html => { :class => "form-horizontal" } do |f|
      = f.inputs do
        = f.input :email
        = f.input :password
      = f.actions do
        = f.action :submit, :label => "Logg inn"

  .span5
    %h2 Logg inn som administrator
    = semantic_form_for Member.new(:mail => @member_mail), :url => login_path, :html => { :class => "form-horizontal" } do |f|
      = f.inputs do
        = f.input :mail
        = f.input :passord, :as => :password
      = f.actions do
        = f.action :submit, :label => "Logg inn"

%p
  = link_to "Ingen bruker? Opprett ny konto her!", new_user_path

%p
  %a
    = link_to forgot_password_users_path do
      Glemt passord?
