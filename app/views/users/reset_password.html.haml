- title 'Tilbakestill passord'

.row-fluid
  .span12
    %h1 Tilbakestill passord

    = semantic_form_for @user, :url => reset_password_user_url(:id => @user, :hash => @hash), :method => :post, :html => { :class => "form-horizontal" } do |f|
      = f.inputs do
        = f.input :password
        = f.input :password_confirmation
      = f.actions do
        = f.action :submit, :label => "Tilbakestill passord"
